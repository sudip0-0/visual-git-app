{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Default desktop capability for opening and validating local Git repository folders.",
  "windows": ["main"],
  "permissions": ["dialog:allow-open"]
}
